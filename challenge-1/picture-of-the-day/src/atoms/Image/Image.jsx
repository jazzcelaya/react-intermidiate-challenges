import styled from 'styled-components';

const Image = styled.div`
  display: inline-block;

  img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    max-height: 75vh;
  }
`;

export default Image;
